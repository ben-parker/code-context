# CodeContext worker SDK for Node.js

Dependency-free Content-Length framing, a streaming message decoder, protocol method
constants, and a write helper for implementing a CodeContext language worker.

The authoritative wire contract is `protocol/parser-protocol.schema.json` in a
CodeContext release. See `docs/language-worker-sdk.md` in the repository for lifecycle
and graph-ownership rules.
